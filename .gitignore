.Rproj.user
.Rhistory
.RData
.Ruserdata

# Quarto render artifacts (root and per-folder caches, incl. the Typst font cache)
/.quarto/
.quarto/
*.html
*_files/

# sources (raw recordings and transcripts - contain candid internal notes)
sources/

# audio never belongs in this repo, anywhere (moved to ~/archive/aguasan-audio/)
*.m4a

# file formats (the brand extension's Word reference document is the one exception)
*.docx
!proposal/_extensions/openwashdata/owd/reference.docx

# review exports (docx + CSVs), regenerated from source by the render/extract
# scripts; the scripts are tracked, their output is not
exports/

.DS_Store
